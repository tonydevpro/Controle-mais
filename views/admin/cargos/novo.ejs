<!DOCTYPE html>
<html lang="pt-br">
<head>
  <meta charset="UTF-8">
  <title>Criar Novo Cargo</title>
  <link rel="stylesheet" href="/css/style.css">
  <style>
    body {
      font-family: Arial, sans-serif;
      padding: 20px;
    }
    label {
      display: block;
      margin-top: 8px;
    }
    input[type="text"] {
      width: 300px;
      padding: 6px;
      margin-top: 4px;
    }
    fieldset {
      margin-top: 15px;
      padding: 10px;
      border: 1px solid #ccc;
    }
    legend {
      font-weight: bold;
    }
    button {
      margin-top: 15px;
      padding: 8px 16px;
      background-color: #215a6d;
      color: white;
      border: none;
      border-radius: 4px;
      cursor: pointer;
    }
    button:hover {
      background-color: #3ca2a2;
    }
    a {
      display: inline-block;
      margin-top: 15px;
      text-decoration: none;
      color: #215a6d;
    }
    a:hover {
      text-decoration: underline;
    }
  </style>
</head>
<body>

  <h1>Criar Novo Cargo</h1>

  <form action="/admin/cargos/novo" method="POST">
    <label for="nome">Nome do Cargo:</label>
    <input type="text" id="nome" name="nome" required>

    <fieldset>
      <legend>Permissões:</legend>
      <% if (permissoes.length === 0) { %>
        <p>Nenhuma permissão disponível.</p>
      <% } else { %>
        <% permissoes.forEach(p => { %>
          <label>
            <input type="checkbox" name="permissoes" value="<%= p.id %>">
            <%= p.nome %>
          </label>
        <% }) %>
      <% } %>
    </fieldset>

    <button type="submit">Criar Cargo</button>
  </form>

  <p><a href="/admin/cargos">← Voltar à lista de cargos</a></p>

</body>
</html>
